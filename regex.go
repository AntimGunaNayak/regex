package regex

import(
	"fmt"
)

func TestHello(){
	// purpose of this is to check if external go file can call this module via the github path ( learning in progress - don't judge )
	fmt.Println("hello world")
}